node_modules/
.next/
out/
*.log
.DS_Store
next-env.d.ts
tsconfig.tsbuildinfo

# the private half of the forge stays home — committing it would publish
# exactly what it hides. The generic logic (forge-data.mjs) is public.
scripts/forge-private.mjs
